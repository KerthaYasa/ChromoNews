pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2
sentence-transformers>=2.2.0
google-generativeai>=0.5.0
streamlit>=1.30.0
Sastrawi>=1.0.1
python-dotenv>=1.0.0

# --- Untuk Textual Similarity (BERTScore, seluruh kalimat artikel) ---
# Dipakai di extraction/text_similarity.py untuk anti-duplikasi WHAT/WHY/HOW
# dan validasi hasil sistem vs AI (evaluation). Model default multilingual:
# "bert-base-multilingual-cased" (di-download otomatis dari HuggingFace saat
# pertama dipakai). Jika gagal load, otomatis fallback ke cosine similarity
# SentenceTransformer, lalu word-overlap.
bert-score>=0.3.13

# --- Untuk POS Tagging Bahasa Indonesia (deteksi verba, ekstraksi HOW) ---
# Dipakai di extraction/pos_tagger.py. Model "w11wo/indonesian-roberta-base-
# posp-tagger" di-download otomatis dari HuggingFace saat pertama dipakai
# (reuse transformers+torch yang sudah ada di requirements ini untuk NER).
# Jika gagal load, otomatis fallback ke heuristik morfologi afiks verba
# Indonesia (me-/ber-/di-/ter-/per- ... -kan/-nya).

# --- Untuk fitur NER model (WHO/WHERE extraction) ---
# Opsional secara fungsional: kalau gagal install/load, app otomatis
# fallback ke rule-based murni (lihat extraction/ner_model.py)
transformers>=4.41.0

# WAJIB untuk model cahya/xlm-roberta-large-indonesian-NER -- tokenizer
# XLM-RoBERTa pakai SentencePiece, BEDA dari WordPiece-nya BERT lama.
# Tanpa ini, load_ner_pipeline() akan gagal dengan error import sentencepiece.
sentencepiece>=0.1.99

# CATATAN soal torch:
# transformers menolak load checkpoint format lama (pytorch_model.bin)
# kalau torch < 2.6 (proteksi CVE-2025-32434). Model NER baru pakai format
# safetensors jadi aman, tapi tetap disarankan torch>=2.6.0 untuk kompatibilitas.
torch>=2.6.0

# Untuk GPU NVIDIA (opsional, lebih cepat):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
